pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = uri("https://repo.onarandombox.com/multiverse-releases")
        }
    }
}

rootProject.name = "multiverse-core"

